package com.example.system.search;


import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.example.system.search")
public class SystemSearchApplication {
    public static void main(String[] args) {
        SpringApplication.run(SystemSearchApplication.class, args);
    }
}
